---
name: manga-forge
description: 根据自然语言指令生成多页漫画。从故事设计、以 OMNY（漫画分镜 YAML）设计分格、交给图像生成 AI 作画，到回收和质检，形成一条完整流水线。用户提到“画漫画”“N 页漫画”“四格漫画”“故事漫画”等需求时启用。
---

# manga-forge — 从自然语言渲染漫画原稿的技能

负责将用户的一句话转化为**完成的漫画页面 PNG**。

~~~
用户指令
  ↓ ① 编写故事（本技能负责）
  ↓ ② 用 OMNY 设计分格（本技能负责，最重要）
  ↓ ③ 通过 scripts/forge.sh 渲染（作画交给 codex）
  ↓ ④ 回收（collect.sh）
  ↓ ⑤ 逐页目视质检（本技能负责）
  ↓ ⑥ 交付（deliver.sh，送至用户指定位置）
page1.png … pageN.png
~~~

## 🚨 开始前必须阅读

1. **[docs/pitfalls.md](docs/pitfalls.md)**：常见陷阱全集。**不读就会踩进同样的坑。**
2. **[prompts/ndm-v10.md](prompts/ndm-v10.md)**：OMNY 的完整写法规范，包括 schema、分格模式与 9 种气泡。**编写 OMNY 前必须阅读。**
3. **[omay/standard.omay.yaml](omay/standard.omay.yaml)**：提供给作画端的规则；理解其内容可提高分镜精度。

---

## ① 故事

- 默认 **2～5 页**；若用户指定页数则遵从。约 10 页可一次渲染。
- 每页建议 **3～6 格**。
- 先划定“第几页”的边界，再设计**起承转合与高光时刻**（关键台词、情绪落点）。
- 用户描述较少时，可从最近对话和上下文中提取题材。

## ② OMNY 设计（本技能核心）

按 prompts/ndm-v10.md 的 schema 编写 YAML。重点如下：

- 分格以 **A4（210 × 297 mm）绝对坐标**定义；id 按右上至左下的阅读顺序排列。
- 🚨 **每页必须使用不同的分格模式。** 禁止全部页面采用同一骨架（如“大上格 → 右格 → 左格 → 大下格”）；相邻页面不可使用相同模式。
- 所有台词必须放入 `bubbles` 数组。**每个气泡 20～40 个汉字**，长台词应拆开。
- 使用 `shape` 指定气泡形状（normal / thought / square / caption / flash / uniflash / wobbly / whisper / handwritten）。
- 每格都要指定背景细节等级 `bg`（-1/0/1/2）。**同页混用 0/1/2 是理想状态**；禁止全格 2，且 bg:2 每页最多 2 格。
- 🧍 **人物居中原则**：对话格中把人物放大置于画面中央，气泡分布在人物右侧 → 左侧。气泡会碰到脸时，**移动气泡，不要把人物挪到角落。**
- 内心独白只用 `monologue: true` 表示。**不要把圆括号“（）”写进文本**（括号也会被画进图中）。
- 关键台词和情绪落点使用“面部特写”的大格，并在 `action` 中写明。
- 在 `materials` 列出所需素材（人物、背景、小物），并在每格的 `assets` 中引用。即使缺少素材，作画 AI 也会根据 `note` 自由设计，因此不要中断。

⚠️ **bbox 表达的是“意图”，不是“最终版式”。**（见 docs/pitfalls.md 的陷阱 6）
作画 AI 会优先保证构图自然，并可能调整格子位置。这是正常行为，**偏移不等于损坏**。
若后续流程需要精确格子坐标，应从完成原稿重新测量。

每部作品的 OMNY 都应保存为文件（例如 `<日期>_<标题>.omny.yaml`）。
它是可复用资产，未来可用于换画风重印及后续制作。

## ③ 渲染

~~~bash
./scripts/forge.sh -n <OMNY 文件> [-r <立绘 1>] [-r <立绘 2>] [-m "作品专属补充指令"]
~~~

- `-r` 参考图建议**最多 3 张**；过多会降低每张图的影响力。
- `-m` 用于写**画风要求**，或“绝不能省略这个角色特征”等作品专属提醒。
- **超时时间 = 页数 × 120 秒 + 余量。** 10 页作品通常需 20 分钟以上。
- 🚨 **渲染中不要运行其他 codex 调用。** `resume --last` 是全局的，可能混入别的作品（陷阱 3）。
- 需要换画风时，将 omay/styles.md 中的提示词放在 OMNY 的 `meta.style_notes` 第一行。

## ④ 回收

forge.sh 完成后会自动运行 collect.sh，将页面按 page1.png … 的形式存入 out/<作品名>/。

⚠️ **图像数量可能与页数不一致**（作画 AI 有时会自行重绘；见陷阱 5）。
若出现警告，请查看每张 PNG 右上角的页码（如“3/10”）后手动排序。

## ⑤ 质检（不可省略）

**逐页打开实际查看。** 检查以下项目：

- 页面没有缺失（检查页眉的“N/M”）。
- 台词清晰可读，角色没有认错。
- 服装和发型在跨页时保持一致。
- **季节、时段与服装、背景之间没有矛盾**（陷阱 9）。
- 气泡尾巴指向正确说话者。
- 预期的无声格能仅凭画面成立。

轻微的文字波动属于生成特性，可接受。只需使用**同一份 OMNY 重新请求有问题的页面**（不要添加额外演出指令；直接重试往往更容易修正）。

## ⑥ 交付（质检后）

将通过质检的原稿交给用户。

- **用户指定了交付位置时**（如“放在桌面”“保存到 ~/manga”）：

~~~bash
./scripts/deliver.sh -f out/<作品名> -t <指定位置>/<作品名> -n <OMNY 文件>
~~~

  - 使用 `-n` 同时交付分镜（OMNY），使其作为资产保存。
  - 交付目录存在此前结果时，脚本会停止。只有在用户确认允许覆盖后才能加 `--force`。
- **用户未指定位置时**，不复制文件，直接将 out/<作品名>/ 作为交付位置。
- 无论哪种情况，最后都必须报告：
  - 完成页面的完整路径。
  - 质检摘要（检查结果；若重生成过页面也需说明）。
- 🚨 不得在完成 ⑤ 质检之前交付。图像数量不一致时，未重新排序的文件不可交付。

---

## 补充

### 更换画风

omay/styles.md 提供 12 组提示词（水彩绘本、蜡笔、柔粉彩、彩铅、剪纸、水墨、毛毡、彩绘玻璃、木版画、厚涂油画、撕纸、孔版印刷）。
想大幅改变效果时，诀窍是**指定“绘画媒材 + 支撑材料（纸张）”**。
加上“禁止动画式上色”“不要半成品般精致”等提醒，可让画风更彻底。

### 将素材沉淀为资产

角色、背景和小物一旦确定设计，就**保存为图像并在下次作为参考图使用**，有助于保持世界观一致。
详见 docs/characters.md。

### 出错时

检查 out/<作品名>/forge.log 中是否有 generated_images/<UUID>/…png 的记录。
若没有，通常是**只做了声明而未生成**（陷阱 1）；可用相同命令重新执行。
