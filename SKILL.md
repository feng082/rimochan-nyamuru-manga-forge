---
name: manga-forge
description: 在 Codex 中根据自然语言创作、编辑或渲染多页漫画。使用 OMNY 分镜与 OMAY 规则，适用于“画漫画”“N 页漫画”“四格漫画”“故事漫画”等请求；不适用于单张非漫画插图。
---

# manga-forge — Codex 原生漫画工作流

将用户的故事需求转化为可编辑的 OMNY 分镜，并在 Codex 中生成、检查和交付漫画页面。

默认使用当前 Codex 任务中的图像生成能力；本仓库的 `scripts/forge.sh` 是**可选的 Codex CLI 批处理模式**，用于需要本地 `out/` PNG 文件或可复现命令行运行的场景。

## 先决定工作范围

- 用户只要分镜、OMNY、故事或版式建议时，只完成相应内容，不生成图像。
- 用户要求生成、重绘或导出漫画页面时，执行完整的设计、渲染和质检流程。
- 页数未指定时，默认建议 2～5 页；每页 3～6 格。

## 开始前阅读

1. 每次编写或修改 OMNY 前，阅读 [OMNY 规范](prompts/ndm-v10.md)。
2. 每次原生渲染前，阅读 [OMAY 规则](omay/standard.omay.yaml)。
3. 仅在使用 CLI 批处理模式时，阅读 [CLI 常见陷阱](docs/pitfalls.md)。
4. 用户提供参考图、希望复用角色或需要设计背景时，阅读 [角色与背景素材](docs/characters.md)。
5. 用户要求更换画风时，阅读 [画风调色板](omay/styles.md)。

## Codex 原生流程

1. 从用户需求设计故事节奏与页面结构，再将分镜保存为 `<日期>_<标题>.omny.yaml`。必须遵守 OMNY 规范中的页数、阅读顺序、分格差异、气泡、人物布局和 `bg` 规则。
2. 需要渲染时，在**当前任务中实际调用**图像生成工具；不得只给出计划或声明。
3. 一次只生成一页。按页码串行生成，向每页提示提供该页 OMNY 内容、适用的 OMAY 规则、作品画风和参考图。不要并行图像生成，也不要在同一作品中混用不相关的角色参考。
4. 逐页检查：页码、台词、气泡尾巴、角色一致性、季节与服装、无声格的可读性。发现问题时，只重生成对应页面。
5. 若图像生成工具返回本地路径，报告路径；若只返回内联成品，则按实际情况交付图片，不得虚构已保存的 PNG 路径。

`bbox` 表达的是布局意图，不是完成原稿的精确像素坐标。若后续工作需要精确格子坐标，必须从成稿重新测量。

## 可选：Codex CLI 批处理模式

当用户明确需要 `out/<作品名>/page*.png`、命令行批量运行或可复现实验时，使用：

```bash
./scripts/forge.sh -n <OMNY 文件> [-r <参考图>] [-m "作品专属补充指令"]
```

- `forge.sh` 通过 `codex` CLI 逐页生成并自动运行 `collect.sh`。
- 参考图 `-r` 建议最多 3 张；`-m` 用于画风和不可省略的作品专属特征。
- CLI 模式使用 `resume --last` 保持跨页一致性，因此其运行期间不得启动其他 `codex` 图像生成任务。
- 仅在质检通过后，且用户明确要求复制到指定位置时，使用 `scripts/deliver.sh`；目标已存在时，必须先取得覆盖授权才能使用 `--force`。

## 交付

交付时说明：

- 已生成的页面及其实际位置或内联成品；
- OMNY 文件位置（若已创建）；
- 质检结果，以及是否重生成过任何页面。
